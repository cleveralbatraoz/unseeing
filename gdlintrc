# gdlint configuration (godot-gdscript-toolkit). Defaults apply except where
# pinned explicitly here, so every machine lints identically.
max-line-length: 100
# observer_test.gd crossed the 1000-line default during the superface
# outline campaign's Task 6 fix round (per-suite ground-truth coverage,
# not sprawl — see game/tests/observer_test.gd's own max-public-methods
# note for the sibling reason this suite already runs long). Raised with
# headroom rather than nudged exactly to fit, so the next task's tests
# don't reopen this file to bump it again by ten lines.
max-file-lines: 1200
